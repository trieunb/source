import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 10,
        backgroundColor: '#fff'
    },
    btnLogin: {
        marginTop: 15
    },
    input: {
        paddingLeft: 15,
        paddingRight: 15
    }
});

export default styles
